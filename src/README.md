# src/

Код появится здесь после DEVELOPMENT GATE (см. `AGENTS.md` §5).

До gates разрешена только reversible technical foundation: tooling, CI skeleton, application shell, mock API boundaries — без финальной доменной schema и feature implementation.
